# Provider Metadata

This folder stores optional per-provider interface metadata.

- `openai.yaml`: metadata for OpenAI/Codex-style loaders.
- `claude.yaml`: metadata for Claude-style loaders.

For other tools, add another `*.yaml` file with:
- `interface.display_name`
- `interface.short_description`
- `interface.default_prompt`
