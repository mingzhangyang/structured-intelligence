# Provider Metadata

This folder stores provider-specific interface metadata for the `source-summarizer` skill.

Required baseline providers:
- `openai.yaml`
- `claude.yaml`

Additional providers can be added as extra `*.yaml` files with the same
`interface` fields.
